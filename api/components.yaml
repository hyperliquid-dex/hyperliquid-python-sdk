components:
  schemas:
    FloatString:
      type: string
      pattern: "^\\d+\\.?\\d*$"
    Address:
      type: string
      pattern: "^0x[a-fA-F0-9]{40}$"
    AssetPosition:
      type: object
    MarginSummary:
      type: object
    L2Level:
      type: object
